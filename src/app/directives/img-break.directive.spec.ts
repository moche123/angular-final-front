import { ImgBreakDirective } from './img-break.directive';

describe('ImgBreakDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgBreakDirective();
    expect(directive).toBeTruthy();
  });
});
